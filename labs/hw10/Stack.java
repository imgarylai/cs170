/* -----------------------------------------------------------
   Implement a stack object containg Strings

   Use an array of String to implement the stack.
   You will also need these variables:

        1. top = points to the top of the stack
        2. nItems = number of items on the stack

   (If you are careful, you can use "top" to count the number 
    of items in the stack. Up to you)
   ----------------------------------------------------------- */

public class Stack
{

   /* -------------------------------------
      Put your instance variables here.
      They must be private
      ------------------------------------- */


   /* -----------------------------------------------------------
      Define a constructor "Stack(int size)" method to create
      a stack that can contain "size" Strings in the stack
      ----------------------------------------------------------- */



   /* -----------------------------------------------------------
      Define the method "push(String s)" method that push
      the string "s" onto the stack 

      Remember, "push" is an instance method. So it has
      a hidden parameter "this".

      The type if the parameter "this" is always:  

               *** Stack *** (because we are inside class Stack)

      The parameter "this" points to a Stack object which
      has the instance variables !

      The "push(String s)" method returns void
      ----------------------------------------------------------- */

   /* -----------------------------------------------------------
      Define the method "pop()" method that pop the String at
      the top of the stack and returns it.

      Remember, "pop" is an instance method. So it has
      a hidden parameter "this".

      The type if the parameter "this" is always:  

               *** Stack *** (because we are inside class Stack)

      The parameter "this" points to a Stack object which
      has the instance variables !

      The "pop()" method returns a String
      ----------------------------------------------------------- */



   /* -----------------------------------------------------------
      Define the method "size()" that returns the number of
      items stored in the stack.

      Remember, "size()" is an instance method. So it has
      a hidden parameter "this".

      The type if the parameter "this" is always:

               *** Stack *** (because we are inside class Stack)

      The parameter "this" points to a Stack object which
      has the instance variables !

      The "size()" method returns an integer
      ----------------------------------------------------------- */




   /* -----------------------------------------------------------
      Define the method "toString()" method that let us print
      the stack.

      Remember, "toString" is an instance method. So it has
      a hidden parameter "this".

      The type if the parameter "this" is always:

               *** Stack *** (because we are inside class Stack)

      The parameter "this" points to a Stack object which
      has the instance variables !

      You should concatenate ** all the items in the stack **
      into **one string** and RETURN that string.

      Remember: the "toString()" method returns a String !!!
      ----------------------------------------------------------- */

}
